% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Correlation.R
\name{ScatterPlot}
\alias{ScatterPlot}
\title{Create a Scatter Plot with Correlation Statistics}
\usage{
ScatterPlot(
  data,
  x_var,
  y_var,
  pointsize = 1.5,
  alpha = 0.6,
  add = "reg.line",
  line_color = "#1F78B4",
  fill_color = "#A6CEE3",
  linesize = 1,
  conf = TRUE,
  cor_method = "spearman",
  label.x.d = 0.1,
  label.y.d = 0.1,
  Xlab,
  Ylab,
  title,
  textsize = 4
)
}
\arguments{
\item{data}{A data frame containing the variables to be plotted.}

\item{x_var}{Character string specifying the column name for the x-axis variable.}

\item{y_var}{Character string specifying the column name for the y-axis variable.}

\item{pointsize}{Numeric value specifying the size of points. Default is 1.5.}

\item{alpha}{Numeric value between 0 and 1 specifying point transparency. Default is 0.6.}

\item{add}{Character string specifying the type of line to add (e.g., "reg.line", "none"). Default is "reg.line".}

\item{line_color}{Character string specifying the color of the regression line. Default is "#1F78B4".}

\item{fill_color}{Character string specifying the fill color for confidence interval. Default is "#A6CEE3".}

\item{linesize}{Numeric value specifying the thickness of the regression line. Default is 1.}

\item{conf}{Logical value indicating whether to display confidence interval. Default is TRUE.}

\item{cor_method}{Character string specifying correlation method: "pearson", "spearman", or "kendall". Default is "spearman".}

\item{label.x.d}{Numeric value for horizontal positioning of correlation label (as proportion of x-range from minimum). Default is 0.1.}

\item{label.y.d}{Numeric value for vertical positioning of correlation label (as proportion of y-range from maximum). Default is 0.1.}

\item{Xlab}{Character string for x-axis label.}

\item{Ylab}{Character string for y-axis label.}

\item{title}{Character string for plot title.}

\item{textsize}{Numeric value specifying the text size of correlation annotation. Default is 4.}
}
\value{
A ggplot object representing the scatter plot with correlation statistics.
}
\description{
Generates a scatter plot with optional regression line, confidence interval, 
and correlation coefficient annotation using ggpubr and ggplot2.
}
\examples{
\dontrun{
# Create sample data
set.seed(123)
df <- data.frame(
  height = rnorm(100, 170, 10),
  weight = rnorm(100, 70, 15)
)

# Basic scatter plot with Spearman correlation
ScatterPlot(data = df, x_var = "height", y_var = "weight",
            Xlab = "Height (cm)", Ylab = "Weight (kg)",
            title = "Height vs Weight")

# Customized plot with Pearson correlation
ScatterPlot(data = df, x_var = "height", y_var = "weight",
            pointsize = 2, alpha = 0.8,
            line_color = "darkred", fill_color = "lightcoral",
            cor_method = "pearson",
            Xlab = "Height (cm)", Ylab = "Weight (kg)",
            title = "Correlation Analysis")
}
}
